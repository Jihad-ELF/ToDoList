/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
    //search for JavaScript and TypeScript files (js, jsx, ts, tsx)
    // within the src directory and its subdirectories (./src/**/*).
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}